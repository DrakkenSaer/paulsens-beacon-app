<%= form_for(beacon) do |f| %>
  <% if beacon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(beacon.errors.count, "error") %> prohibited this beacon from being saved:</h2>

      <ul>
        <% beacon.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :uuid %>
    <%= f.text_field :uuid %>
  </div>

  <div class="field">
    <%= f.label :major_uuid %>
    <%= f.text_field :major_uuid %>
  </div>

  <div class="field">
    <%= f.label :minor_uuid %>
    <%= f.text_field :minor_uuid %>
  </div>


  <h3>Notifications</h3>
  <%= f.fields_for :notifications do |builder| %>
    <%= render 'notifications_fields', f: builder %>
  <% end %>

  <%= link_to_add_fields 'Add Notifaction', f, :notifications %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>