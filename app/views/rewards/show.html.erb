<p id="notice"><%= notice %></p>

<p>
  <strong><%= @reward.rewardable_type.titleize %>:</strong>
  <%= @reward.polymorphic_resource.title %>
</p>

<p>
  <strong>User:</strong>
  <%= @reward.user.email %>
</p>

<%= link_to 'Edit', edit_reward_path(@reward) %> |
<%= link_to 'Back', rewards_path %>
