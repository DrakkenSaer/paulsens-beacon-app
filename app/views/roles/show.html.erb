<p>
  <strong>Name:</strong>
  <%= @role.name %>
</p>

<p>
  <strong>Resource:</strong>
  <%= @role.resource_type.blank? ? "N/A" : @role.resource_type %>
</p>

<p>
  <strong>Resource:</strong>
  <%= @role.resource_id.blank? ? "N/A" : @role.resource_id %>
</p>

<%= link_to 'Edit', edit_role_path(@role) %>
<%= link_to 'Back', roles_path %>
