<div class="field">
  <%= f.label :id, :promotion %>
  <%= f.collection_select :id, @promotions, :id, :title, include_blank: true %>
</div>

<% unless f.object.new_record? %>
  <div>
    <%= f.label :_destroy, 'Remove:' %>
    <%= f.check_box :_destroy %>
  </div>
<% end %>

